{% extends 'base.html.twig' %} 

{% block title %}Personne{% endblock %}

{% block body %}
    <h1>Personne</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th class="table-active">Id</th>
                <td>{{ personne.id }}</td>
            </tr>
            <tr >
                <th class="table-active" >Nom</th>
                <td>{{ personne.nom }}</td>
            </tr>
            <tr>
                <th class="table-active" >Date_nais</th>
                <td>{{ personne.dateNais ? personne.dateNais|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th class="table-active">Cin</th>
                <td>{{ personne.cin }}</td>
            </tr>
            
             
             <tr>
             <th class="table-active">voitures</th>
             <tr ></tr>
            {% for voiture in personne.getMatricule() %}
              
                <td colspan="2"style="text-align:center">  {{voiture.getId()}}  </td>    
             </tr>
                {% endfor %}
        </tbody>
    </table>
    
    <a href="{{ path('app_personne_index') }}">back to list</a>

    <a href="{{ path('app_personne_edit', {'id': personne.id}) }}">edit</a>

    {{ include('personne/_delete_form.html.twig') }}
{% endblock %}
